<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transactions | Inventory System</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        :root { --purple-theme: #5d3fd3; --bg-light: #f8f9fc; --text-main: #4e5154; --text-muted: #6e707e; }
        body { background-color: var(--bg-light); font-family: 'Inter', sans-serif; font-size: 14px; color: var(--text-main); display: flex; margin: 0; }
        
        .sidebar { width: 260px; height: 100vh; background: #fff; position: fixed; border-right: 1px solid #eee; padding-top: 20px; }
        .sidebar-brand { padding: 0 25px 30px; }
        .sidebar-brand h5 { font-size: 18px; font-weight: 700; color: var(--purple-theme); margin: 0; }
        .nav-link { color: var(--text-muted); padding: 12px 25px; display: flex; align-items: center; text-decoration: none; font-weight: 500; border-radius: 0 50px 50px 0; margin-right: 15px; }
        .nav-link i { margin-right: 15px; width: 20px; text-align: center; font-size: 1.1rem; }
        .nav-link.active { background: var(--purple-theme); color: white !important; }

        .header-container { margin-left: 260px; width: calc(100% - 260px); }
        .top-nav { height: 70px; background: white; display: flex; align-items: center; justify-content: space-between; padding: 0 40px; border-bottom: 1px solid #eee; }
        .main-content { padding: 30px 40px; }

        .card { border: none; border-radius: 15px; box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.05); background: white; margin-bottom: 25px; padding: 25px; }
        .form-label { font-size: 12px; font-weight: 600; color: var(--text-muted); }
        .form-control, .form-select { background-color: #f8f9fc; border: 1px solid #e3e6f0; font-size: 13px; padding: 10px 15px; }
        
        .table thead th { font-size: 12px; text-transform: uppercase; color: var(--text-muted); border-bottom: 2px solid #f8f9fc; padding: 15px; }
        .table tbody td { padding: 15px; font-size: 13px; vertical-align: middle; border-bottom: 1px solid #f8f9fc; }
        
        /* Badges matching your Sale/Stock In design */
        .badge-sale { background: var(--purple-theme); color: white; padding: 4px 10px; border-radius: 6px; font-size: 11px; }
        .badge-stockin { background: #5d3fd3; opacity: 0.8; color: white; padding: 4px 10px; border-radius: 6px; font-size: 11px; }
        .badge-stockout { background: #9d81f5; color: white; padding: 4px 10px; border-radius: 6px; font-size: 11px; }
    </style>
</head>
<body>

<div class="sidebar">
    <div class="sidebar-brand"><h5>Inventory & Sales</h5><small class="text-muted">Management System</small></div>
    <nav class="nav flex-column">
        <a href="/" class="nav-link"><i class="bi bi-grid-fill"></i> Dashboard</a>
        <a href="/products" class="nav-link"><i class="bi bi-box-seam"></i> Products</a>
        <a href="/suppliers" class="nav-link"><i class="bi bi-truck"></i> Suppliers</a>
        <a href="/pos" class="nav-link"><i class="bi bi-cart3"></i> POS</a>
        <a href="/inventory" class="nav-link"><i class="bi bi-house-door"></i> Inventory</a>
        <a href="/barcode" class="nav-link"><i class="bi bi-qr-code-scan"></i> Barcode Scanner</a>
        <a href="/transactions" class="nav-link active"><i class="bi bi-file-earmark-text"></i> Transactions</a>
        <a href="/reports" class="nav-link"><i class="bi bi-graph-up-arrow"></i> Reports</a>
        <a href="/users" class="nav-link"><i class="bi bi-people"></i> Users</a>
    </nav>
</div>

<div class="header-container">
    <div class="top-nav">
        <h5 class="mb-0 fw-bold">Transactions</h5>
        <div class="d-flex align-items-center gap-3">
            <span class="text-muted" style="font-size: 13px;">Welcome, <strong>System Admin</strong></span>
            <a href="/logout" class="btn btn-sm btn-outline-secondary px-3">Logout</a>
        </div>
    </div>

    <div class="main-content">
        <div class="card">
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label">Search</label>
                    <input type="text" class="form-control" placeholder="Search by product or user...">
                </div>
                <div class="col-md-6">
                    <label class="form-label">Transaction Type</label>
                    <select class="form-select">
                        <option>All</option>
                        <option>Sale</option>
                        <option>Stock In</option>
                    </select>
                </div>
            </div>
        </div>

        <div class="card">
            <h6 class="fw-bold mb-4">Transaction History (<%= records.length %> records)</h6>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Product</th>
                            <th>User</th>
                            <th>Type</th>
                            <th>Quantity</th>
                            <th>Amount</th>
                            <th>Date</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% records.forEach(item => { %>
                        <tr>
                            <td><%= item.id %></td>
                            <td><strong><%= item.product %></strong></td>
                            <td><%= item.user %></td>
                            <td>
                                <span class="<%= item.type === 'Sale' ? 'badge-sale' : (item.type === 'Stock In' ? 'badge-stockin' : 'badge-stockout') %>">
                                    <%= item.type %>
                                </span>
                            </td>
                            <td><%= item.qty %></td>
                            <td><%= item.amount %></td>
                            <td class="text-muted"><%= item.date %></td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

</body>
</html>