<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Monitoring | Inventory & Sales</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    <style>
        :root { 
            --purple-theme: #5d3fd3; 
            --bg-light: #f8f9fc;
            --text-main: #4e5154;
            --text-muted: #6e707e;
            --font-size-standard: 14px;
        }

        body { 
            background-color: var(--bg-light); 
            font-family: 'Inter', sans-serif; 
            font-size: var(--font-size-standard);
            color: var(--text-main);
            margin: 0; 
            display: flex; 
        }

        /* Unified Sidebar */
        .sidebar { 
            width: 260px; 
            height: 100vh; 
            background: #fff; 
            position: fixed; 
            border-right: 1px solid #eee; 
            padding-top: 20px; 
            z-index: 1000; 
        }
        .sidebar-brand { padding: 0 25px 30px; }
        .sidebar-brand h5 { font-size: 18px; font-weight: 700; color: var(--purple-theme); }

        .nav-link { 
            color: var(--text-muted); 
            padding: 12px 25px; 
            display: flex; 
            align-items: center; 
            text-decoration: none; 
            font-size: var(--font-size-standard); 
            font-weight: 500;
            border-radius: 0 50px 50px 0; 
            margin-right: 15px; 
            transition: 0.2s;
        }
        .nav-link i { margin-right: 15px; width: 20px; text-align: center; font-size: 1.1rem; }
        .nav-link:hover { color: var(--purple-theme); background: #f4f0ff; }
        .nav-link.active { background: var(--purple-theme); color: white !important; }

        /* Main Content Layout */
        .header-container { margin-left: 260px; width: calc(100% - 260px); }
        .top-nav { 
            height: 70px; 
            background: white; 
            display: flex; 
            align-items: center; 
            justify-content: space-between; 
            padding: 0 40px; 
            border-bottom: 1px solid #eee; 
        }

        .main-content { padding: 40px; }
        .card { border: none; border-radius: 15px; box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.05); background: white; margin-bottom: 25px; }
        
        /* Table Styling */
        .table thead th { 
            font-size: 12px; 
            text-transform: uppercase; 
            color: var(--text-muted); 
            font-weight: 600;
            border: none;
            padding: 15px;
        }
        .table tbody td { padding: 15px; font-size: 14px; border-bottom: 1px solid #f8f9fc; }
        
        .badge-stock { background-color: var(--purple-theme); color: white; border-radius: 6px; padding: 5px 12px; font-size: 12px; }

        /* Updated Logout Button to match image */
        .logout-btn { 
            border: 1px solid #7e8299; 
            padding: 8px 24px; 
            border-radius: 6px; 
            text-decoration: none; 
            color: #4b5563; 
            font-size: 15px;
            transition: all 0.2s;
        }
        .logout-btn:hover {
            background-color: #f8f9fa;
            color: #333;
        }
        
        .welcome-msg {
            font-size: 16px;
            color: #7e8299;
        }
        .welcome-msg strong {
            color: #3f4254;
        }
    </style>
</head>
<body>

    <div class="sidebar">
        <div class="sidebar-brand">
            <h5 class="mb-0">Inventory & Sales</h5>
            <small class="text-muted">Management System</small>
        </div>
        <nav class="nav flex-column">
            <a href="/" class="nav-link"><i class="bi bi-grid-fill"></i> Dashboard</a>
            <a href="/products" class="nav-link"><i class="bi bi-box-seam"></i> Products</a>
            <a href="/suppliers" class="nav-link"><i class="bi bi-truck"></i> Suppliers</a>
            <a href="/pos" class="nav-link"><i class="bi bi-cart3"></i> POS</a>
            <a href="/inventory" class="nav-link active"><i class="bi bi-house-door"></i> Inventory</a>
            <a href="/barcode" class="nav-link"><i class="bi bi-qr-code-scan"></i> Barcode Scanner</a>
            <a href="/transactions" class="nav-link"><i class="bi bi-journal-text"></i> Transactions</a>
            <a href="/reports" class="nav-link"><i class="bi bi-graph-up-arrow"></i> Reports</a>
            <a href="/users" class="nav-link"><i class="bi bi-people"></i> Users</a>
        </nav>
    </div>

    <div class="header-container">
        <div class="top-nav">
            <h5 class="mb-0 fw-bold" style="font-size: 18px;">Inventory Monitoring</h5>
            <div class="user-info d-flex align-items-center gap-4">
                <div class="welcome-msg">
                    <span class="text-muted" style="font-size: 13px;">Welcome, <strong>System Admin</strong></span>
                    <a href="/logout" class="btn btn-sm btn-outline-secondary px-3" style="font-size: 12px;">Logout</a>
                </div>
            </div>
        </div>
        <div class="main-content">
            <div class="mb-4">
                <p class="text-muted">Monitor and track your inventory levels across all categories.</p>
            </div>

            <div class="card p-4 mb-4">
                <h6 class="fw-bold mb-3" style="font-size: 14px;">Filters</h6>
                <div class="row g-3">
                    <div class="col-md-5">
                        <label class="small fw-bold mb-2 text-muted">Search Product</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-0"><i class="bi bi-search"></i></span>
                            <input type="text" id="invSearch" class="form-control bg-light border-0" style="font-size: 14px;" placeholder="Search name or barcode...">
                        </div>
                    </div>
                    <div class="col-md-3">
                        <label class="small fw-bold mb-2 text-muted">Category</label>
                        <select class="form-select bg-light border-0" style="font-size: 14px;">
                            <option selected>All Categories</option>
                            <option>Electronics</option>
                            <option>Furniture</option>
                        </select>
                    </div>
                    <div class="col-md-4">
                        <label class="small fw-bold mb-2 text-muted">Stock Status</label>
                        <select class="form-select bg-light border-0" style="font-size: 14px;">
                            <option selected>All Statuses</option>
                            <option>In Stock</option>
                            <option>Low Stock</option>
                        </select>
                    </div>
                </div>
            </div>

            <div class="card p-4">
                <h6 class="fw-bold mb-4" style="font-size: 14px;">Inventory Details (<%= products.length %> Items)</h6>
                <div class="table-responsive">
                    <table class="table align-middle">
                        <thead>
                            <tr>
                                <th>Product Details</th>
                                <th>Category</th>
                                <th>Current Stock</th>
                                <th>Min. Threshold</th>
                                <th>Status</th>
                                <th>Price</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% products.forEach(product => { %>
                            <tr>
                                <td>
                                    <div class="fw-bold"><%= product.product_name %></div>
                                    <div class="text-muted" style="font-size: 12px;"><%= product.barcode %></div>
                                </td>
                                <td><%= product.category %></td>
                                <td class="fw-bold"><%= product.stock %></td>
                                <td><%= product.threshold || 5 %></td>
                                <td>
                                    <% if(product.stock > (product.threshold || 5)) { %>
                                        <span class="badge-stock">In Stock</span>
                                    <% } else { %>
                                        <span class="badge bg-danger text-white" style="font-size: 11px; padding: 5px 10px;">Low Stock</span>
                                    <% } %>
                                </td>
                                <td class="fw-bold">₱<%= product.price %></td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

</body>
</html>