<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management | Inventory System</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    <style>
        :root { 
            --purple-theme: #5d3fd3; 
            --bg-light: #f8f9fc; 
            --text-main: #4e5154; 
            --text-muted: #6e707e; 
        }

        body { 
            background-color: var(--bg-light); 
            font-family: 'Inter', sans-serif; 
            font-size: 14px; /* Standard Dashboard Font Size */
            color: var(--text-main); 
            display: flex; 
            margin: 0; 
        }

        /* Sidebar Styling - Exactly like Dashboard */
        .sidebar { width: 260px; height: 100vh; background: #fff; position: fixed; border-right: 1px solid #eee; padding-top: 20px; }
        .sidebar-brand { padding: 0 25px 30px; }
        .sidebar-brand h5 { font-size: 18px; font-weight: 700; color: var(--purple-theme); margin: 0; }
        
        .nav-link { 
            color: var(--text-muted); 
            padding: 12px 25px; 
            display: flex; 
            align-items: center; 
            text-decoration: none; 
            font-weight: 500; 
            transition: 0.3s;
        }
        .nav-link i { margin-right: 15px; width: 20px; text-align: center; font-size: 1.1rem; }
        .nav-link:hover { color: var(--purple-theme); background: #f4f0ff; border-radius: 0 50px 50px 0; margin-right: 15px; }
        .nav-link.active { 
            background: var(--purple-theme); 
            color: white !important; 
            border-radius: 0 50px 50px 0; 
            margin-right: 15px; 
        }

        /* Main Layout */
        .header-container { margin-left: 260px; width: calc(100% - 260px); }
        .top-nav { 
            height: 70px; 
            background: white; 
            display: flex; 
            align-items: center; 
            justify-content: space-between; 
            padding: 0 40px; 
            border-bottom: 1px solid #eee; 
        }

        .main-content { padding: 30px 40px; }

        /* Card & Table Styling */
        .card { 
            border: none; 
            border-radius: 15px; 
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.05); 
            background: white; 
            padding: 25px; 
        }

        .table thead th { 
            font-size: 12px; 
            text-transform: uppercase; 
            letter-spacing: 0.05em;
            color: var(--text-muted); 
            border-bottom: 2px solid #f8f9fc; 
            padding: 15px; 
        }
        .table tbody td { padding: 15px; vertical-align: middle; border-bottom: 1px solid #f8f9fc; }

        .btn-add { 
            background: var(--purple-theme); 
            color: white; 
            border: none; 
            padding: 10px 20px; 
            border-radius: 10px; 
            font-weight: 600; 
            font-size: 13px;
        }
        
        /* Action Buttons */
        .btn-edit { color: #4e73df; background: #eaecf4; border: none; padding: 5px 10px; border-radius: 5px; margin-right: 5px; }
        .btn-delete { color: #e74a3b; background: #f8d7da; border: none; padding: 5px 10px; border-radius: 5px; }
    </style>
</head>
<body>

    <div class="sidebar">
        <div class="sidebar-brand">
            <h5>Inventory & Sales</h5>
            <small class="text-muted">Management System</small>
        </div>
        <nav class="nav flex-column">
            <a href="/" class="nav-link"><i class="bi bi-grid-fill"></i> Dashboard</a>
            <a href="/products" class="nav-link"><i class="bi bi-box-seam"></i> Products</a>
            <a href="/suppliers" class="nav-link"><i class="bi bi-truck"></i> Suppliers</a>
            <a href="/pos" class="nav-link"><i class="bi bi-cart3"></i> POS</a>
            <a href="/inventory" class="nav-link"><i class="bi bi-house-door"></i> Inventory</a>
            <a href="/barcode" class="nav-link"><i class="bi bi-qr-code-scan"></i> Barcode</a>
            <a href="/transactions" class="nav-link"><i class="bi bi-file-earmark-text"></i> Transactions</a>
            <a href="/reports" class="nav-link"><i class="bi bi-graph-up-arrow"></i> Reports</a>
            <a href="/users" class="nav-link active"><i class="bi bi-people"></i> Users</a>
        </nav>
    </div>

    <div class="header-container">
        <div class="top-nav">
            <h5 class="mb-0 fw-bold">User Management</h5>
            <div class="d-flex align-items-center gap-3">
                <span class="text-muted" style="font-size: 13px;">Welcome, <strong>System Admin</strong></span>
                <a href="/logout" class="btn btn-sm btn-outline-secondary px-3" style="font-size: 12px;">Logout</a>
            </div>
        </div>

        <div class="main-content">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h6 class="fw-bold mb-0">System Users</h6>
                <button class="btn-add" data-bs-toggle="modal" data-bs-target="#addUserModal">
                    <i class="bi bi-plus-lg me-1"></i> Add New User
                </button>
            </div>

            <div class="card">
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Username</th>
                                <th>Role</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(item => { %>
                            <tr>
                                <td><strong><%= item.name %></strong></td>
                                <td><%= item.username %></td>
                                <td><span class="badge bg-light text-dark"><%= item.role %></span></td>
                                <td>
                                    <button class="btn-edit"><i class="bi bi-pencil-square"></i></button>
                                    <button class="btn-delete"><i class="bi bi-trash"></i></button>
                                </td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="addUserModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content" style="border-radius: 15px; border: none;">
                <div class="modal-header border-0 p-4">
                    <h5 class="modal-title fw-bold" style="color: var(--purple-theme);">Create New User</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <form action="/users/add" method="POST">
                    <div class="modal-body px-4 pb-4">
                        <div class="mb-3">
                            <label class="form-label small fw-bold text-muted">FULL NAME</label>
                            <input type="text" name="name" class="form-control" placeholder="Enter full name" required style="background: #f8f9fc;">
                        </div>
                        <div class="mb-3">
                            <label class="form-label small fw-bold text-muted">USERNAME</label>
                            <input type="text" name="username" class="form-control" placeholder="Enter username" required style="background: #f8f9fc;">
                        </div>
                        <div class="mb-3">
                            <label class="form-label small fw-bold text-muted">ROLE</label>
                            <select name="role" class="form-select" style="background: #f8f9fc;">
                                <option value="Admin">Admin</option>
                                <option value="Manager">Manager</option>
                                <option value="Cashier">Cashier</option>
                            </select>
                        </div>
                        <div class="mb-3">
                            <label class="form-label small fw-bold text-muted">PASSWORD</label>
                            <input type="password" name="password" class="form-control" placeholder="Enter password" required style="background: #f8f9fc;">
                        </div>
                    </div>
                    <div class="modal-footer border-0 p-4">
                        <button type="button" class="btn btn-light" data-bs-dismiss="modal">Cancel</button>
                        <button type="submit" class="btn-add">Save User Account</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>